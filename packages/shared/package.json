{
  "name": "@acs/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared contracts for AWS Console Switcher: Result/FlowError types, extension & Native Messaging protocol discriminated unions, and non-secret data models. Passwords/TOTP are never representable at the type level.",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
