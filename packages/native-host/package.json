{
  "name": "@acs/native-host",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "AWS Console Switcher Native Messaging host (bw CLI wrapper). Skeleton only; implemented in task 2.",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@acs/shared": "*"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
